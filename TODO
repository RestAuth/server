0.6.1:
* Fix compatablity issues with Pytho2.6 (@skipUnless)
* Have setup.py test(server) use its own ready-to-use setting file
* If Debug=False and request.is_secure() is also false, fail!


Future:

* add support for /users/<user>/blobs
