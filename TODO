0.6.1:
* restauth-import should still use the new backend
* move RestAuth.Users.hashes to RestAuth.Users.hashers
* Documentation is still outdated in some places.
  * How-To write custom hashers rewrite
  * Split backends and writing backends.
* Optional support for storing passwords in a cache.
* Add UWSGI files to documentation, so they can be referenced
* kill the last epydoc related documentation
* move cli-scripts to RestAuth/bin
* Make content type handlers global objects that are never reloaded
  (like password hashers)
* make RestAuth.backends.utils.*_backend a variable, not a function

Future:

* add support for /users/<user>/blobs
