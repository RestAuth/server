0.6.0:

* In groups we seem to use only() to get too many fields.

Future:

* add support for /users/<user>/blobs

When Django >= 1.4 is required:
* Remove backported override_settings decorator
* Use Django password hashers
* Remove custom implementation of get_random_string in Users.models
