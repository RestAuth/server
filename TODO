0.6.0:

* Add a config-variable that enables the admin interface
* Remove date_joined and last_login database fields, they should be
  implemented as a property
* Automatically set the 'date joined' property upon user-creation
* Merge algorithm|salt|hash into one column, to become compatible with regular
  Django password hashers in 1.4

Future:

* add support for /users/<user>/blobs
