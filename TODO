0.6.1:
* Update Debian/Ubuntu installation instructions on supported distributions
* Add ability for backend modules to load modules at first use (like password
  hashers), good for Redis backend.
* Go through documentation in localsettings.py


Future:

* add support for /users/<user>/blobs
